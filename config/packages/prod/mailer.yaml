framework:
    mailer:
        # Doit correspondre à la boîte SMTP Hostinger (SPF / auth)
        envelope:
            sender: '%env(default:mail_envelope_default:MAIL_ENVELOPE_SENDER)%'

parameters:
    env(MAIL_ENVELOPE_SENDER): 'contact@ndamstore.sowcoder.com'
    mail_envelope_default: 'contact@ndamstore.sowcoder.com'
